import Depiction from "../components/Depiction"

export default function YoutubeBGPlayback() {
	return (
	<Depiction
		name="YoutubeBGPlayback"
		github="https://github.com/YulkyTulky/YoutubeBGPlayback"
		subtitle="Play YouTube in the background for free!"
	>
		
		<p>
		YoutubeBGPlayback is a super lightweight tweak, being only one line of code! Check it out through the GitHub link below!
		</p>

		<p>
		Updated for the latest builds of Youtube.
		</p>

	</Depiction>
	)
}